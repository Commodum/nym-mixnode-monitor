{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "NymApiBaseUrl": "https://validator.nymtech.net/",
  "MixnodeId": "0", // set
  "MixnodeIp": "127.0.0.1",  // assume that the container is running on the same host as the mixnode - can be overriden with IP
  "MixnodeApiPort": "8000",
  "MixnodeScheme": "http"

}
